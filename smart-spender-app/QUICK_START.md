# 🚀 Quick Start Guide

## За да стартирате приложението ВЕДНАГА:

### 1️⃣ Навигирайте към проекта
```bash
cd smart-spender-app
```

### 2️⃣ Инсталирайте dependencies (ако вече не е направено)
```bash
npm install
```

### 3️⃣ Създайте .env файл
```bash
# За Windows PowerShell:
Copy-Item .env.example .env

# За Mac/Linux:
cp .env.example .env
```

### 4️⃣ (Опционално) Добавете Claude API Key
Отворете `.env` файла и добавете вашия API ключ:
```
EXPO_PUBLIC_CLAUDE_API_KEY=sk-ant-api03-xxxxxxxx
```

**Забележка**: Можете да пропуснете този стъп за начало и да тествате само UI.

### 5️⃣ Стартирайте приложението
```bash
npm start
```

### 6️⃣ Изберете платформа
След стартиране на Metro Bundler:

**За iOS Simulator:**
- Натиснете `i` в терминала

**За Android Emulator:**
- Натиснете `a` в терминала

**За физическо устройство:**
- Инсталирайте "Expo Go" от App Store / Google Play
- Сканирайте QR кода от терминала

## ✅ Какво можете да тествате ВЕДНАГА:

1. **Навигация** - Всички 5 таба работят
2. **Scanner екран** - Камера и image picker функционират
3. **UI и дизайн** - Всички екрани са оформени

## 🔧 Ако срещнете проблем:

### Metro Bundler не стартира:
```bash
npm start -- --reset-cache
```

### Пакети липсват:
```bash
npm install
```

### iOS Simulator не се отваря:
```bash
npm run ios
```

### Android Emulator проблеми:
```bash
npm run android
```

## 📱 Следващи стъпки:

След като потвърдите, че всичко работи:

1. Прочетете **NEXT_STEPS.md** за детайлен план
2. Вижте **PROJECT_SUMMARY.md** за обобщение
3. Започнете с интеграцията на Claude API в Scanner

## 💡 Полезни команди:

```bash
# Рестарт на development server
npm start

# Clear cache и restart
npm start -- --reset-cache

# Build за iOS
npm run ios

# Build за Android
npm run android

# Проверка на dependencies
npm list

# Update на Expo SDK
npx expo install --fix
```

## 🎯 За разработка:

**Горещи модули:**
- Промените в кода се прилагат автоматично
- Не е нужно да рестартирате приложението

**Debugging:**
- Shake устройството или натиснете Ctrl+M (Android) / Cmd+D (iOS)
- Изберете "Debug" за Chrome DevTools

**Logs:**
- Всички console.log се виждат в терминала

---

**Готови ли сте? Стартирайте `npm start` и започнете! 🚀**
